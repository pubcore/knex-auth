{
  "scripts": {
    "test": "nyc --reporter=html --reporter=text mocha --inspect --require babel-register --recursive",
    "build": "babel src --out-dir js",
    "prepare": "npm run build",
    "watch": "babel --watch src --out-dir js",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "main": "js/index.js",
  "license": "MIT",
  "babel": {
    "presets": [
      "env"
    ],
    "plugins": [
      "transform-object-rest-spread"
    ]
  },
  "name": "@pubcore/knex-auth",
  "version": "1.0.0",
  "description": "Functions to persist authentication state to a RDMS via knex",
  "repository": {
      "type": "git",
      "url": "git+ssh://git@github.com/pubcore/knex-auth.git"
  },
  "directories": {
    "test": "test"
  },
  "keywords": [
    "basic authentication"
  ],
  "author": "pubcore",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "chai-datetime": "^1.5.0",
    "coveralls": "^3.0.2",
    "eslint": "^4.19.1",
    "eslint-plugin-mocha": "^5.0.0",
    "mocha": "^5.2.0",
    "moment-timezone": "^0.5.21",
    "nyc": "^12.0.2",
    "sqlite3": "^4.0.1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jshashes": "^1.0.7",
    "knex": "^0.14.6",
    "prop-types": "^15.6.2"
  }
}
